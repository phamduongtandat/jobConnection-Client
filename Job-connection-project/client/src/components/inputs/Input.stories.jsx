import Input from './Input';

const meta = {
  component: Input,
  title: 'inputs/Input',
};

export const Primary = {
  name: 'Input',
  args: {
    placeholder: 'Tên tài khoản',
    type: 'text',
    hasError: false,
    className: '',
  },
};

export default meta;
